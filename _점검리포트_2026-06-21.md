# WCIF 2026 홈페이지 프로젝트 무결성 점검 리포트

점검일: 2026-06-21 (라이브 배포 점검 추가)
대상: WCIF 2026 정적 웹사이트 (한국어 13p + 영어 12p, Supabase 참가자 등록 연동)
라이브 URL: https://wcif.world/

---

## ★ 라이브 배포 점검 결과 (https://wcif.world/)

### 🔴 핵심: 도메인 첫 화면이 Claude 사이트가 아님

`https://wcif.world/` (루트)로 접속하면 **Claude로 만든 사이트가 아니라 예전 WordPress 사이트**가 나옵니다.
(WordPress 7.0 기반, 메뉴 구조가 /introduce·/program/theme·/speaker·/video·/media 로 전혀 다르고, 이미지가 /wp-content/ 에서 로드됨.)

Claude로 만든 정적 사이트는 **서버에 업로드는 되어 있으나** 주소에 `.html`을 직접 붙여야만 열립니다.

| 주소 | 결과 |
|---|---|
| `wcif.world/` | ❌ 옛 WordPress 사이트 (기본 첫 화면) |
| `wcif.world/index.html` | ✅ Claude 사이트 (로컬과 일치) |
| `wcif.world/register.html` | ✅ Claude 사이트 (로컬과 일치) |
| `wcif.world/speakers.html` | ✅ Claude 사이트 (로컬과 일치) |
| `wcif.world/en/index.html` | ✅ Claude 사이트 (로컬과 일치) |

원인: 서버가 `/` 요청에 WordPress의 `index.php`를 우선 보여주고, 우리 `index.html`은 뒤로 밀린 상태입니다.
즉 **파일 매칭(로컬↔서버)은 정상**이지만 **도메인 루트가 새 사이트를 가리키지 않습니다.**

조치 방향 (택1, 호스팅 환경 확인 필요)
1. WordPress를 새 사이트로 교체할 계획이면: WordPress를 내리고 정적 파일을 루트로 서비스하도록 설정.
2. 당분간 병행 운영이면: `.htaccess` 의 DirectoryIndex 우선순위를 `index.html` 로 변경하거나, 루트에서 새 사이트로 리다이렉트.
3. 새 사이트를 별도 경로/서브도메인(예: `2026.wcif.world`)으로 운영.

### ✅ 보안: 관리자 비밀 파일은 공개되지 않음 (양호)

라이브 서버에서 직접 확인한 결과:

- `wcif.world/js/admin-secret.js` → 응답 없음(미배포) ✅
- `wcif.world/admin.html` → 응답 없음(미배포) ✅
- `wcif.world/supabase_setup.sql` → 응답 없음(미배포) ✅
- `wcif.world/js/content.js`, `js/reg-config.js`(anon 키, 공개 정상) → 정상 제공 ✅

지난 점검의 🔴 긴급 항목(비밀키 유출 위험)은 **실제 배포에서는 안전하게 제외**되어 있음을 확인했습니다.
(단, 향후 폴더 통째 재업로드 시 다시 포함되지 않도록 주의는 계속 필요.)

---

## 종합 결론

구조와 링크는 매우 양호합니다. **깨진 링크 0건**, 한국어-영어 페이지 정합성 완벽.
다만 **배포 방식에 따라 치명적 정보 유출 위험이 있는 항목 1건**을 발견했습니다. 아래 [긴급] 항목을 먼저 확인해 주세요.

| 심각도 | 항목 | 건수 |
|---|---|---|
| 🔴 긴급 | 관리자 비밀키(admin-secret.js) 배포 노출 위험 | 1 |
| 🟡 주의 | Git 저장소 미설정 (.gitignore가 작동하지 않는 상태) | 1 |
| 🟡 주의 | 기본 관리자 비밀번호 평문 노출 | 1 |
| 🟢 정리 | 미사용(고아) 이미지 파일 | 약 78 |

---

## 🔴 긴급: 관리자 비밀키 유출 위험

`js/admin-secret.js` 파일에 다음 민감 정보가 평문으로 들어 있습니다.

- `SERVICE_ROLE_KEY` — Supabase 관리자 키 (DB 전체 조회/수정/삭제 가능)
- `PRIVATE_KEY` — 참가자 휴대전화 번호 복호화용 개인키
- `ADMIN_PW_HASH` — 관리자 비밀번호 해시

이 파일과 `admin.html`은 `.gitignore`에 등록돼 있지만, **`.gitignore`는 Git을 통해 배포할 때만 작동합니다.** FTP 업로드, Netlify/Vercel 폴더 드래그, cPanel 파일 업로드 등 **폴더를 통째로 올리는 방식이면 이 파일들도 함께 공개 웹에 올라갑니다.**

만약 공개되면: 누구나 개인키로 **전체 참가자 휴대전화 번호를 복호화**하고, 서비스 롤 키로 **등록 DB를 전부 조회·삭제**할 수 있습니다.

권장 조치
1. 배포 시 `admin.html`, `js/admin-secret.js`, `supabase_setup.sql`이 **절대 업로드되지 않도록** 배포 폴더에서 분리 (별도 로컬 폴더에 보관).
2. 관리자 페이지는 웹 배포 대신 로컬에서만 열어 사용.
3. 만에 하나 이미 한 번이라도 공개 서버에 올라간 적이 있다면, Supabase에서 **서비스 롤 키 재발급(rotate)** 및 **RSA 키쌍 교체**를 진행.

---

## 🟡 주의

### Git 저장소 미설정
현재 폴더는 Git 저장소가 아닙니다(`.git` 없음). 즉 `.gitignore` 파일은 있지만 **아무 보호 기능도 하지 않는 상태**입니다. 중요한 프로젝트인 만큼 버전 관리를 시작하시길 권장합니다. `git init` 시점에 `.gitignore`가 비로소 비밀 파일을 자동 제외합니다.

### 기본 관리자 비밀번호 노출
`js/admin-secret.js` 주석에 초기 비밀번호(`wcif2026!admin`)가 평문으로 적혀 있습니다. 실제 운영 비밀번호로 변경했는지 확인하고, 주석의 평문 비밀번호는 삭제하는 것이 안전합니다.

---

## 🟢 정리 권장: 미사용 이미지

배포에는 영향 없으나, webp로 교체되며 남은 원본/구버전 이미지가 다수 있습니다.

- `images/speakers/` 하위 webp 48개 — 실제로는 `images/speakers/card/`의 webp를 사용 (구버전 폴더로 추정)
- `images/sponsor/01_문체부.png` 등 한글명 PNG 11개 — 실제로는 `sponsor-01.webp` 사용
- `images/home_front/KV1~3.png` 3개 — 실제로는 `kv1~3.webp` 사용
- `images/host/00_*.png`, `_preview.png` — 실제로는 `kcif-logo.webp` 사용
- 루트의 `kv_square.png`(약 410KB) — `images/kv_square.webp`와 중복된 떠도는 파일
- 기타 `images/kv_square.png`, `images/kv_mobile.webp`, `poster_futurestage.png`, `HACKATHON_KV_new.png` 등

`새 사진/` 폴더(연사 원본 사진 11장)는 의도적으로 `.gitignore` 처리된 원본 보관함이므로 정상입니다.

> 참고: 위 파일들은 삭제해도 사이트 동작에 영향이 없습니다. 다만 원본 보관 목적이라면 `새 사진/`처럼 배포 제외 폴더로 옮기는 방식을 권장합니다.

---

## ✅ 정상 확인 항목

- **깨진 링크 0건**: 모든 `href`/`src`/CSS `url()` 참조가 실제 파일로 연결됨. (`render.js`의 `hqdefault.jpg`는 YouTube 썸네일 주소로, 정상)
- **한국어-영어 정합성 완벽**: 루트 12개 일반 페이지 ↔ `en/` 12개 1:1 대응. (`admin.html`만 한국어 전용 — 정상)
- **언어 전환 링크 정상**: 모든 페이지에 KR↔EN 상호 링크 존재. `en/` 페이지는 `../css`, `../js`를 올바르게 참조.
- **공개 페이지의 관리자 파일 참조 없음**: 일반 페이지에서 `admin.html`/`admin-secret.js`를 부르지 않음.
- **등록 보안 설계 양호**: 공개 페이지는 익명(anon) 키만 사용, RLS 정책은 insert만 허용(조회/수정/삭제 차단). 휴대전화는 RSA 암호화 저장.

---

## 다음 단계 제안 (원하시면 진행)

1. Git 저장소 초기화 + 첫 커밋 (비밀 파일 자동 제외 확인)
2. 미사용 이미지 정리 (삭제 대신 `_archive/` 폴더로 이동하는 안전한 방식)
3. 배포용 폴더와 관리자용 폴더 분리 가이드 작성
